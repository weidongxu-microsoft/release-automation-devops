// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.dev.implementation;

import com.azure.dev.fluent.models.LogCollectionInner;
import com.azure.dev.fluent.models.LogInner;
import com.azure.dev.models.Log;
import com.azure.dev.models.LogCollection;
import com.azure.dev.models.SignedUrl;
import java.util.Collections;
import java.util.List;
import java.util.stream.Collectors;

public final class LogCollectionImpl implements LogCollection {
    private LogCollectionInner innerObject;

    private final com.azure.dev.DevManager serviceManager;

    LogCollectionImpl(LogCollectionInner innerObject, com.azure.dev.DevManager serviceManager) {
        this.innerObject = innerObject;
        this.serviceManager = serviceManager;
    }

    public List<Log> logs() {
        List<LogInner> inner = this.innerModel().logs();
        if (inner != null) {
            return Collections
                .unmodifiableList(
                    inner.stream().map(inner1 -> new LogImpl(inner1, this.manager())).collect(Collectors.toList()));
        } else {
            return Collections.emptyList();
        }
    }

    public SignedUrl signedContent() {
        return this.innerModel().signedContent();
    }

    public String url() {
        return this.innerModel().url();
    }

    public LogCollectionInner innerModel() {
        return this.innerObject;
    }

    private com.azure.dev.DevManager manager() {
        return this.serviceManager;
    }
}
